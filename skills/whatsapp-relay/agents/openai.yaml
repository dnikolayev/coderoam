interface:
  display_name: "WhatsApp Relay"
  short_description: "Use coderoam active inbox safely"
  default_prompt: "Use $whatsapp-relay to check, drain, watch, or notify through the local WhatsApp coderoam relay."

policy:
  allow_implicit_invocation: true
